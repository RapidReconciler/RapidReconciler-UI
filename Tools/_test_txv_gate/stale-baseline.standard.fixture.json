{
  "_comment": [
    "FIXTURE for the baseline-existence rule. Identical to valid.standard.fixture.json",
    "except the format baseline names NOSUCHCARD, which is not in any config fixture.",
    "The standard has always claimed the list is validated against the catalog; nothing",
    "validated it until 2026-08-15, so a code for a renamed or deleted card sat there",
    "exempting nothing and reading as coverage. This fixture is that rule's proof."
  ],
  "headings": {
    "order": [
      { "field": "checked",     "heading": "What happened",             "maxBullets": 2 },
      { "field": "context",     "heading": "Not tested on these rows",  "maxBullets": 2 },
      { "field": "found",       "heading": "What I found",              "maxBullets": 3 },
      { "field": "fix",         "heading": "What to do",                "maxBullets": 2 },
      { "field": "alsoChecked", "heading": "Also checked",              "maxBullets": 5 }
    ]
  },
  "bulletWordLimit": 25,
  "bannedPhrases": { "phrases": [] },
  "formatBaseline": { "cards": [{ "card": "NOSUCHCARD", "fields": "*" }] }
}
